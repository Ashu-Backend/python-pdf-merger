This is a simple Python script that merges multiple PDF files into a single PDF document.
The script automatically scans the current folder for all PDF files and combines them into one merged file using the PyPDF2 library.
It is useful for quickly merging reports, notes, or documents without using external software.
